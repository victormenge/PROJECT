#include <stdio.h>
#include <stdlib.h>

int main()
{
    void bubble_sort()
    a = [12,9,14,5,1]
    n = len(a)
    for i in range(n):
        for j in range(0, n-i-1):
            if a[j] > a[j+1]:
                a[j], a[j+1] = a[j+1], a[j]

bubble_sort()
print(a)
    return 0;
}
